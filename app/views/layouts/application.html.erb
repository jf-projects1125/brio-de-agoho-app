<!DOCTYPE html>
<html>
  <head>
    <title>BrioDeAgohoApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-screen flex justify-center items-center">
    <main>
       <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>

        <% if affiliate_signed_in? %>
          <%= link_to "Sign out", destroy_affiliate_session_path, data: { 'turbo-method' => :delete, 'turbo_confirm' => "Are you sure?"  }, class: "hover:bg-green-500 hover:text-white-50 rounded py-2 px-4" %>
        <% end %>
        
      <%= yield %>
    </main>
  </body>
</html>
